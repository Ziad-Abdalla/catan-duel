# heavy / re-derivable scratch — keep the repo lean (URLs + scripts reproduce these)
sources/
art-staging/
montages/
probe/

# intermediate generated data (derived from transcripts)
generated/
pages.json
rules-work/_*-fulltext.txt
